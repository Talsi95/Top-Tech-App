version: '3.8'

services:
  # שירות ה-frontend ישמש רק כבונה (builder)
  frontend:
    build: ./frontend
    command: npm run build
    volumes:
      - ./frontend:/app
      - /app/node_modules

  # שירות ה-backend
  backend:
    build: ./backend
    ports:
      - "5001:5000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    command: npm run dev
    env_file:
      - ./backend/.env
    environment:
      - NODE_ENV=development
    depends_on:
      - frontend

  # שירות Nginx
  nginx:
    image: nginx:1.21-alpine
    ports:
      - "80:80"
    volumes:
      # העתקת קובץ הקונפיגורציה של Nginx
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      # החלק הקריטי: שיתוף תיקיית ה-dist עם Nginx
      - ./frontend/dist:/usr/share/nginx/html:ro
    depends_on:
      - frontend
      - backend
